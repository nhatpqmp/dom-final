import { Ora } from 'ora';
/**
 * Set up credentials file for Google cloud
 *
 * @param {string|undefined} credentials
 * @param {Ora} spinner
 * @return {Promise<void>}
 */
export declare function setupCredentials(spinner: Ora, credentials?: string): Promise<void>;
