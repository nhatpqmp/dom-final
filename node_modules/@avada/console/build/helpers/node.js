"use strict";
exports.__esModule = true;
exports.checkNodeVersion = void 0;
/**
 * Check node version for allowing to enter to the application
 *
 * @param {number} version
 */
function checkNodeVersion(version) {
    var currentNodeVersion = process.versions.node;
    var semver = currentNodeVersion.split('.');
    var major = semver[0];
    if (major < version) {
        // tslint:disable-next-line:no-console
        console.error("You are running Node " + currentNodeVersion + ". Please update your version of node to " + version + " or higher to continue.");
        process.exit(1);
    }
}
exports.checkNodeVersion = checkNodeVersion;
