interface Options {
    port: string;
    forward: string;
    credentials?: string;
}
/**
 * Serve application for enable https
 *
 * @param {Object} options
 */
export default function serveApp(options: Options): Promise<void>;
export {};
