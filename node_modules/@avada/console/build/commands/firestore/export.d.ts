interface Options {
    credentials?: string;
    file?: string;
}
/**
 * Export all items from collection to a file
 *
 * @see https://raw.githubusercontent.com/dalenguyen/firestore-import-export/master/export.js
 * @param {String} collection
 * @param {Options} options
 */
export default function exportFirestore(collection: string, options: Options): Promise<void>;
export {};
