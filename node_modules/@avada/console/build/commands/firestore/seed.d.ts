interface Options {
    credentials?: string;
    target?: string;
    number: string;
}
/**
 * Run seed for generating the testing data
 *
 * @param {object} options
 */
export default function seedFirestore(options: Options): Promise<void>;
export {};
