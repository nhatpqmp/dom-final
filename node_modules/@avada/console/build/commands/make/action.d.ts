interface Options {
    group: string;
}
/**
 * Create a new action file for React with preset
 *
 * @param {string} name
 * @param {Options} options
 * @return {Promise<void>}
 */
export default function makeAction(name: string, options: Options): Promise<void>;
export {};
